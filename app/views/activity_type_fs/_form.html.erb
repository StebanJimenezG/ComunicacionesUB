<%= form_for(@activity_type_f) do |f| %>
  <% if @activity_type_f.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@activity_type_f.errors.count, "error") %> prohibited this activity_type_f from being saved:</h2>

      <ul>
      <% @activity_type_f.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :id_activity %><br>
    <%= f.number_field :id_activity %>
  </div>
  <div class="field">
    <%= f.label :id_type_f %><br>
    <%= f.number_field :id_type_f %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
