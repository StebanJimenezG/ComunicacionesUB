<%= form_for(@comitemember) do |f| %>
  <% if @comitemember.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comitemember.errors.count, "error") %> prohibited this comitemember from being saved:</h2>

      <ul>
      <% @comitemember.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :committee_id %><br>
    <%= f.number_field :committee_id %>
  </div>
  <div class="field">
    <%= f.label :member_id %><br>
    <%= f.number_field :member_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
