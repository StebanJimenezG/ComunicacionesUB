<%= form_for(@type_f) do |f| %>
  <% if @type_f.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@type_f.errors.count, "error") %> prohibited this type_f from being saved:</h2>

      <ul>
      <% @type_f.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :file %><br>
    <%= f.text_field :file %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
